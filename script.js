$(document).ready(function(){
	$('.reputation-wrapper').slick({
			infinite: true,
			arrow: true,
			dots: true,
	});
	$('.beforbegin-contents-wrapper').slick({
		arrows: true,
		dots: true,
		variableWidth: true,
		infinete: false,
	});
});